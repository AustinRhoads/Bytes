const initial_state = {

}



const AppetizerReducer = (state = initial_state, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default AppetizerReducer;